# Leave empty to use the Vite dev proxy (recommended for local dev): requests to
# /api are forwarded to VITE_API_PROXY (default http://localhost:8081).
# For a deployed build, point this at the API origin, e.g. https://api.example.com
VITE_API_BASE=
